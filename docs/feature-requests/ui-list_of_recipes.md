## FEATURE:

Create a mobile app that lists recipes from the backend API. The app should display a list of recipes with their names.
After clicking on a recipe, it should show the details of the recipe including ingredients and instructions.
We are currently in a demo stage so the app can look simple (just text and data) but be functional.
API will be deployed on a local computer.

## EXAMPLES:

None. This should be a straightforward implementation of a mobile app that consumes the existing backend API.

## DOCUMENTATION:

- `docs/mobile/mobile.md`
- `docs/backend/api.md`
- `docs/mobile/ui.md`

## OTHER CONSIDERATIONS:

- use `ip` command to find the local computer's IP address
- don't add any tests, just a simple implementation
- when you're done ask me to run the app and I will review it