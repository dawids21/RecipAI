# Claude Code local settings
settings.local.json

# Google Play service account key (never commit)
scripts/play-service-account.json

# Python virtualenv for release scripts
scripts/.venv/

# Decrypted keystores must never be committed; only the .gpg files are tracked
**/*.jks
**/*-key.properties