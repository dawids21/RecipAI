# Claude Code local settings
settings.local.json

# Google Play service account key (never commit)
scripts/play-service-account.json

# Python virtualenv for release scripts
scripts/.venv/

# Signing keystores live in the password manager and must never be committed
**/*.jks
**/*-key.properties

.env